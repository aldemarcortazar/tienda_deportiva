

const RouterUser = () => {
     const { hash } = location;
    const $contenido = document.getElementById("cont") 
    if(hash === "#/usuario" ){
        
    }else if( true ){

    }
}


export default RouterUser;